PORT=3016
NODE_ENV=production

# SMTP (AWS SES)
SMTP_HOST=email-smtp.us-east-1.amazonaws.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# publicwerx-core system routes
SYSTEM_API_KEY=

# Remote deploy key (shared across fleet)
REMOTE_SYSTEM_KEY=
